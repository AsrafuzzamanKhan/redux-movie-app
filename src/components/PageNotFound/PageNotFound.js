import React from 'react';
import './PageNotFound.scss'

const PageNotFound = () => {
    return (
        <div className='not-found'>
            Not found
        </div>
    );
};

export default PageNotFound;